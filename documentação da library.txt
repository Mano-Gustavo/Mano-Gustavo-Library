# 💎 MANO GUSTAVO UI — DIAMOND EDITION (V7.9)

Uma UI Library moderna, modular e extensível para scripts Roblox.
Projetada para **iniciante usar fácil** e **avançado escalar sem dor**.

---

## [1] CARREGAMENTO (HOSTED / GITHUB)

A library é carregada como **módulo remoto**, evitando scripts gigantes.

```lua
local Library = loadstring(game:HttpGet(
    "https://raw.githubusercontent.com/Mano-Gustavo/Mano-Gustavo-Library/refs/heads/main/library.lua"
))()
```

**Vantagens:**

* Script final menor
* Atualizações sem reenviar scripts
* Estrutura profissional

---

## [2] CRIANDO A JANELA PRINCIPAL

```lua
local Window = Library:CreateWindow({
    Title = "Meu Hub",
    Keybind = Enum.KeyCode.RightControl
})
```

### Recursos da Janela

* Drag & Drop
* Botão Mobile automático
* Sistema de Tabs
* Busca (lupa)
* Tema dinâmico
* Minimizar / Restaurar

---

## [3] ORGANIZAÇÃO: TABS E SECTIONS

### Criar Tabs

```lua
local TabMain = Window:CreateTab("Main")
local TabVisual = Window:CreateTab("Visuals")
```

### Criar Sections (NOVO)

Sections organizam componentes **dentro da Tab**.

```lua
local CombatSection = TabMain:CreateSection("Combat Features")
```

📌 **Importante:**

* Use `Section:CreateX()` para colocar itens dentro da caixa
* Use `Tab:CreateX()` para itens soltos

Sections também respondem ao **Theme System**.

---

## [4] COMPONENTES SUPORTADOS

### A) Button

```lua
local Btn = Section:CreateButton("Reset", function()
    print("Clicked")
end)

Btn:SetTooltip("Executa uma ação única")
```

---

### B) Toggle (com Hooks)

```lua
local Toggle = Section:CreateToggle("Auto Farm", function(Value)
    print(Value)
end, false)

Toggle:OnEnable(function()
    print("Ligou")
end)

Toggle:OnDisable(function()
    print("Desligou")
end)
```

* Salvo automaticamente
* Hooks não misturam lógica

---

### C) Slider

```lua
local Slider = Section:CreateSlider("Speed", 16, 200, 16, function(Value)
    print(Value)
end)
```

---

### D) Dropdown

```lua
local Drop = Section:CreateDropdown("Weapon", {"Gun","Knife"}, function(v)
    print(v)
end)
```

---

### E) Color Picker (RGB)

```lua
local Picker = Section:CreateColorPicker(
    "ESP Color",
    Color3.fromRGB(255,0,0),
    function(Color)
        print(Color)
    end
)
```

Retorna `Color3`.

---

### F) TextBox

```lua
Section:CreateTextBox("Player", "Digite o nome", function(Text)
    print(Text)
end)
```

---

### G) Label

```lua
Section:CreateLabel("----- VISUAL -----")
```

---

## [5] SISTEMA DE CONFIG (SAVE / LOAD)

Salva automaticamente:

* Toggle
* Slider
* Dropdown
* Color Picker

```lua
Library:SaveConfig("Legit")
Library:LoadConfig("Legit")
Library:ResetConfig()
```

📁 Local:

```
workspace/GustavoHub/Configs/*.json
```

⚠️ Requer executor com `writefile` e `readfile`.

---

## [6] SISTEMA DE TEMAS (DYNAMIC THEME)

Altera toda a UI em tempo real:

```lua
Library:SetTheme({
    MainColor = Color3.fromRGB(20,20,20),
    SecondaryColor = Color3.fromRGB(35,35,35),
    AccentColor = Color3.fromRGB(255,0,0),
    TextColor = Color3.fromRGB(255,255,255),
    SectionColor = Color3.fromRGB(30,30,30)
})
```

Afeta:

* Backgrounds
* Textos
* Bordas
* Sliders
* Section Titles

---

## [7] NOTIFICAÇÕES

```lua
Library:Notification({
    Title = "Sucesso",
    Text = "Config carregada",
    Duration = 4,
    Type = "Success"
})
```

Tipos:

* `Success`
* `Error`
* `Warning`
* `Info`

---

## [8] CONCEITOS INTERNOS (IMPORTANTE)

### Flags

* Usam o **nome visível** do componente
* Cada nome deve ser único

```lua
Lib.Flags["Auto Farm"] = true
```

### Options

Permite controle externo:

```lua
Lib.Options["Auto Farm"]:Set(false)
```

📌 **Nota:**
Sistema de `FlagId` está planejado para V8.

---

## [9] USO RECOMENDADO

Ideal para:

* ESP
* Hitbox Expander
* Aimbot UI
* Admin Panels
* Tools Visuais
* Menus Modulares
* E diverços outros

---

## [10] STATUS DA LIBRARY

✔ Modular
✔ Estável
✔ Escalável
✔ Documentada
✔ Pronta para público

---

**Mano Gustavo UI — Diamond Edition V7.9**
Framework de UI, não só um menu.
